{
  "name": "@{{profile.npmjs_organization}}/{{github_prefix}}{{slug}}",
  "description": "{{description}}",
  "license": "{{license}}",
  "author": "{{author.name}} <{{author.email}}> ({{link.home}})",
  "homepage": "{{link.home}}",
  "repository": {
    "type": "git",
    "url": "git+{{repo}}.git"
  },
  "bugs": {
    "email": "{{email.help}}",
    "url": "{{repo}}{{repository.location.issues}}"
  },
  "version": "0.0.1",
  "scripts": {
    "commit": "git-cz",
    "fix": "run-s fix:*",
    "fix:prettier": "prettier --write . && prettier-package-json --write package.json",
    "fix:shellcheck": "find . -type d \\( -name .cache -o -name .git -o -name .modules  -o -name node_modules -o -name .husky -o -name venv \\) -prune -o -type f \\( -name \\*.sh -o -name \\*.sh.j2 \\) -print0 | xargs -0 shellcheck -f diff | git apply",
    "info": "npm-scripts-info",
    "postinstall": "if [ -d .git ]; then husky install; fi",
    "lint:prettier": "prettier --list-different .",
    "lint:shellcheck": "find . -type d \\( -name .cache -o -name .git -o -name .modules  -o -name node_modules -o -name .husky -o -name venv \\) -prune -o -type f \\( -name \\*.sh -o -name \\*.sh.j2 \\) -print0 | xargs -0 -r -n1 shellcheck",
    "lint-staged": "lint-staged",
    "misc:markdown-links": "find . -type d \\( -name .cache -o -name .git -o -name .github -o -name .gitlab -o -name .modules  -o -name node_modules \\) -prune -o -type f \\( -name \\*.md \\) -print0 | xargs -0 -r -n1 markdown-link-check",
    "version": "standard-version --no-verify"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "@megabytelabs/prettier-config": "^1.0.0",
    "cspell": "^5.6.6",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "markdown-link-check": "^3.8.7",
    "npm-scripts-info": "^0.3.9",
    "prettier": "^2.3.1",
    "prettier-package-json": "^2.6.0",
    "prettier-plugin-sh": "^0.6.1",
    "shellcheck": "^1.0.0",
    "standard-version": "^9.3.0"
  },
  "engines": {
    "node": ">=10"
  },
  "funding": [
    {
      "type": "opencollective",
      "url": "{{profile_link.opencollective}}/{{profile.opencollective}}"
    },
    {
      "type": "patreon",
      "url": "{{profile_link.patreon}}/{{profile.patreon}}"
    }
  ],
  "lint-staged": {
    "**/*": [
      "check-merge-conflict",
      "check-added-large-files",
      "check-case-conflict",
      "check-docstring-first",
      "check-executables-have-shebangs",
      "check-symlinks",
      "detect-private-key"
    ],
    "**/*.{js,json,md,ts,yml}": [
      "prettier --write"
    ],
    "**/*.{js}": [
      "npx standard --fix"
    ],
    "**/*.{sh,sh.j2}": [
      "prettier --write",
      "shellcheck"
    ],
    "**/*.xml": [
      "check-xml"
    ],
    "**/*.yml": [
      "check-yaml",
      "yamllint"
    ],
    "package.json": [
      "prettier-package-json --write"
    ],
    "requirements.txt": [
      "requirements-txt-fixer"
    ]
  },
  "prettier": "@megabytelabs/prettier-config",
  "scripts-info": {
    "commit": "Whenever committing code, run 'task commit' - 'task commit' will utilize this step and make your commits better",
    "fix:prettier": "Automatically format files with Prettier",
    "fix:shellcheck": "Attempts to automatically fix shell scripts with Shellcheck",
    "info": "Displays descriptions of all the npm tasks",
    "lint:prettier": "Checks for presence of files that Prettier wants to format differently",
    "lint:shellcheck": "Performs linting of shell scripts with Shellcheck",
    "lint:staged": "Performs many validations on only the current commit's staged files",
    "misc:markdown-links": "Checks markdown files for broken links",
    "prepare-release": "Prepares the repository for a release",
    "version": "Used by 'npm run prepare-release' to update the CHANGELOG"
  },
  "standard-version": {
    "scripts": {
      "prerelease": "git add --all"
    }
  }
}
