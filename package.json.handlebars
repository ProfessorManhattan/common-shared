{
  "name": "@{{profile.npmjs_organization}}/{{github_prefix}}{{slug}}",
  "description": "{{description}}",
  "license": "{{license}}",
  "author": "{{author.name}} <{{author.email}}> ({{link.home}})",
  "homepage": "{{link.home}}",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+{{repo}}.git"
  },
  "bugs": {
    "email": "{{email.help}}",
    "url": "{{repo}}{{repository.location.issues}}"
  },
  "version": "0.0.1",
  "scripts": {
    "commit": "git-cz",
    "postinstall": "if [ -d .git ]; then husky install; fi && if [ -f .husky/pre-commit ]; then chmod 755 .husky/pre-commit; fi",
    "version": "standard-version --no-verify"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "@megabytelabs/prettier-config": "^1.0.0",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^6.0.0",
    "inquirer": "^6.5.2",
    "prettier-plugin-sh": "^0.6.1",
    "signale": "^1.4.0"
  },
  "engines": {
    "node": ">=10"
  },
  "funding": [
    {
      "type": "opencollective",
      "url": "{{profile_link.opencollective}}/{{profile.opencollective}}"
    },
    {
      "type": "patreon",
      "url": "{{profile_link.patreon}}/{{profile.patreon}}"
    }
  ],
  "keywords": [
    "megabytelabs",
    "professormanhattan"
  ],
  "lint-staged": {
    "**/*": [
      "check-merge-conflict",
      "check-added-large-files",
      "check-symlinks"
    ],
    "**/*.(css|html|json|less|md|mjml|php|scss|sh|ts)": [
      "prettier --write"
    ],
    "**/*.js": [
      "npx standard --fix",
      "prettier --write"
    ],
    "**/*.(sh|sh.j2)": [
      "shellcheck"
    ],
    "**/*.xml": [
      "check-xml",
      "prettier --write"
    ],
    "**/*.yml": [
      "check-yaml",
      "yamllint -s",
      "prettier --write"
    ],
    "package.json": [
      "prettier-package-json --write"
    ],
    "requirements.txt": [
      "requirements-txt-fixer"
    ]
  },
  "prettier": "@megabytelabs/prettier-config",
  "standard-version": {
    "scripts": {
      "prerelease": "git add --all"
    }
  }
}
