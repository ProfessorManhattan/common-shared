---
eslint:
  stage: lint
  allow_failure: true
  image:
    name: megabytelabs/eslint:slim
    entrypoint: ['']
  variables:
    GIT_DEPTH: 1
  script:
    - set -eo pipefail
    - eslint --format gitlab . # Requires eslint-formatter-gitlab
