---
pages:
  stage: publish
  allow_failure: true
  variables:
    GIT_DEPTH: 1
  script:
    - set -eo pipefail
    - mv build/docs/ public/
  artifacts:
    paths:
      - public
  only:
    - master
